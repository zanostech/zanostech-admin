# ZanosTech Admin

Next.js admin dashboard scaffold based on `nanabismark1116-admin`.

The folder structure mirrors the reference project, while the visible navigation currently contains only one tab: Dashboard.
